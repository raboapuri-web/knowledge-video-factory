import fs from 'node:fs';import path from 'node:path';const root=path.resolve(import.meta.dirname,'..');const s=JSON.parse(fs.readFileSync(path.join(root,'src/sync-timing.json'),'utf8'));const total=Math.ceil(Number(s.durationSeconds||900)*30),size=Math.ceil(total/8);console.log(JSON.stringify({include:Array.from({length:8},(_,i)=>({id:String(i).padStart(2,'0'),start:i*size,end:Math.min(total-1,(i+1)*size-1)})).filter(x=>x.start<total)}));