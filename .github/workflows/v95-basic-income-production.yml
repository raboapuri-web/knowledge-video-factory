name: V95 Basic Income Production
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['.github/workflows/v95-basic-income-production.yml','shared/bootstrap/v95-basic-income.mjs','shared/v95/**']
permissions: {contents: write}
jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 110
    outputs: {matrix: '${{ steps.plan.outputs.matrix }}'}
    steps:
      - uses: actions/checkout@v4
      - run: node shared/bootstrap/v95-basic-income.mjs
      - run: sudo apt-get update && sudo apt-get install -y ffmpeg fonts-noto-cjk
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - working-directory: v95-basic-income
        run: npm install
      - working-directory: v95-basic-income
        run: ln -sfn "$PWD/node_modules" ../node_modules
      - working-directory: v95-basic-income
        run: node scripts/build-preproduction.mjs
      - name: Fine scene and static-background guard
        run: |
          node - <<'NODE'
          const s=require('./v95-basic-income/src/script-data.json'),d=require('./v95-basic-income/src/scene-data.json');const n=s.beats.length;if(n<270||n>380)throw Error(`scene count ${n}`);if(d.length!==n)throw Error('scene mismatch');if(new Set(s.beats.map(x=>x.visual)).size!==n)throw Error('visual key reuse');const g={},seen=new Set(),phases=new Set();let prev='';for(const x of d){phases.add(x.phase);g[x.bgGroup]=(g[x.bgGroup]||0)+1;if(x.bgGroup!==prev&&seen.has(x.bgGroup))throw Error(`nonconsecutive background reuse ${x.bgGroup}`);seen.add(x.bgGroup);prev=x.bgGroup;}for(const [k,v] of Object.entries(g))if(v>2)throw Error(`background ${k} used ${v} scenes`);if(Object.keys(g).length<140)throw Error(`too few backgrounds ${Object.keys(g).length}`);if(phases.size!==25)throw Error(`expected 25 distinct narrative phases, got ${phases.size}`);const src=require('fs').readFileSync('./shared/v95/scenes.tsx','utf8');for(const p of phases)if(!src.includes('const painter:Record<string'))throw Error(`Missing animated worlds for ${p}`);if(src.includes('useCurrentFrame'))throw Error('Scene engine must not animate backgrounds by frame');console.log(`${n} micro-scenes / ${Object.keys(g).length} unique continuity backgrounds / ${phases.size} bespoke story worlds`);
          NODE
      - working-directory: v95-basic-income
        run: npm run check
      - name: Preview critical original shots
        working-directory: v95-basic-income
        run: |
          mkdir -p out/preview
          for FRAME in 0 900 1800 2700 3600 4500 5400 6300; do
            npx remotion still src/index.tsx V95ScenePreview "out/preview/scene-${FRAME}.png" --frame="$FRAME" --log=error
          done
          test "$(find out/preview -name '*.png' | wc -l)" -eq 8
      - name: Start VOICEVOX
        run: |
          docker pull voicevox/voicevox_engine:cpu-latest
          docker run -d --rm --name voicevox -p 127.0.0.1:50021:50021 voicevox/voicevox_engine:cpu-latest
          for i in {1..90}; do curl -fsS http://127.0.0.1:50021/version && exit 0; sleep 2; done; exit 1
      - working-directory: v95-basic-income
        run: node scripts/generate-voicevox.mjs
      - id: plan
        working-directory: v95-basic-income
        run: echo "matrix=$(node scripts/plan-segments.mjs)" >> "$GITHUB_OUTPUT"
      - run: tar --exclude='v95-basic-income/node_modules' -czf v95-prepared.tar.gz v95-basic-income
      - uses: actions/upload-artifact@v4
        with: {name: v95-prepared, path: v95-prepared.tar.gz, retention-days: 1}
  render:
    needs: prepare
    runs-on: ubuntu-24.04
    timeout-minutes: 100
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix: '${{ fromJson(needs.prepare.outputs.matrix) }}'
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y fonts-noto-cjk
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - uses: actions/download-artifact@v4
        with: {name: v95-prepared}
      - run: tar -xzf v95-prepared.tar.gz
      - working-directory: v95-basic-income
        run: npm install
      - working-directory: v95-basic-income
        run: mkdir -p out && npx remotion render src/index.tsx V95BasicIncome "out/segment-${{ matrix.id }}.mp4" --frames=${{ matrix.start }}-${{ matrix.end }} --codec=h264 --crf=18 --concurrency=2 --log=warn
      - uses: actions/upload-artifact@v4
        with: {name: 'v95-segment-${{ matrix.id }}', path: 'v95-basic-income/out/segment-${{ matrix.id }}.mp4', retention-days: 1}
  finalize:
    needs: [prepare,render]
    runs-on: ubuntu-24.04
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ffmpeg fonts-noto-cjk
      - uses: actions/setup-node@v4
        with: {node-version: 20}
      - uses: actions/download-artifact@v4
        with: {name: v95-prepared}
      - run: tar -xzf v95-prepared.tar.gz
      - uses: actions/download-artifact@v4
        with: {pattern: 'v95-segment-*', path: v95-basic-income/parts, merge-multiple: true}
      - working-directory: v95-basic-income
        run: node scripts/generate-bgm.mjs
      - working-directory: v95-basic-income
        run: |
          mkdir -p out
          find parts -name 'segment-*.mp4' -print | sort -V | while read f; do printf "file '%s'\n" "$(realpath "$f")"; done > out/concat.txt
          ffmpeg -y -loglevel error -f concat -safe 0 -i out/concat.txt -c copy out/video-only.mp4
          ffmpeg -y -loglevel error -i out/video-only.mp4 -i public/audio/narration.m4a -i public/audio/bgm.m4a -filter_complex "[1:a]volume=1[vo];[2:a]volume=.04[bg];[vo][bg]amix=inputs=2:duration=longest[a]" -map 0:v -map "[a]" -c:v copy -c:a aac -b:a 192k -shortest out/v95-final.mp4
          test "$(ffprobe -v error -select_streams v:0 -show_entries stream=width -of default=nw=1:nk=1 out/v95-final.mp4 | head -1)" = '1920'
          test "$(ffprobe -v error -select_streams v:0 -show_entries stream=height -of default=nw=1:nk=1 out/v95-final.mp4 | head -1)" = '1080'
          test "$(ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 out/v95-final.mp4 | cut -d. -f1)" -gt 780
      - run: node shared/qa/generate-scene-contact-sheet.mjs v95-basic-income v95-basic-income/out/v95-final.mp4 v95-basic-income/out/v95-contact-sheet.jpg
      - working-directory: v95-basic-income
        env: {GH_TOKEN: '${{ github.token }}'}
        run: |
          TAG="v95-basic-income-${GITHUB_RUN_ID}"
          NAME="knowledge-video-v95-basic-income-${GITHUB_RUN_ID}.mp4"
          cp out/v95-final.mp4 "out/$NAME"
          gh release create "$TAG" "out/$NAME" out/v95-contact-sheet.jpg preproduction-plan.json qa/preproduction-summary.json SOURCES.md V95_PRODUCTION_SPEC.md --repo "$GITHUB_REPOSITORY" --title "V95 Basic Income Production ${GITHUB_RUN_ID}" --notes "Original basic-income documentary: 25 historical and contemporary worlds, 270-380 narration-aligned micro-scenes, frequent action and scene changes, no nonconsecutive background reuses, fixed adjacent backgrounds with meaningful actor/prop changes, measured VOICEVOX and subtitles, BGM."
