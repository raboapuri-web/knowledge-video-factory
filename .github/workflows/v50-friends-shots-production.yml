name: V50 Friends Fine-Shot Production

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/v50-friends-shots-production.yml'
      - 'shared/bootstrap/v50-friends-shots.mjs'
      - 'shared/v50/**'
      - 'shared/v49/friends-script.txt'
      - 'shared/v49/scenes.tsx'

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-24.04
    timeout-minutes: 100
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
    steps:
      - name: Checkout production repository
        uses: actions/checkout@v4
      - name: Checkout SVG and character template library
        uses: actions/checkout@v4
        with:
          repository: raboapuri-web/test
          ref: main
          path: template-assets
      - name: Materialize V50 production
        run: node shared/bootstrap/v50-friends-shots.mjs
      - name: Install audio tools and Japanese fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-noto-cjk
          fc-cache -f
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install V50 dependencies
        working-directory: v50-friends-shots
        run: npm install
      - name: Expose dependencies to shared tools
        working-directory: v50-friends-shots
        run: ln -sfn "$PWD/node_modules" ../node_modules
      - name: Build preproduction plan
        working-directory: v50-friends-shots
        run: node scripts/build-preproduction.mjs
      - name: Story, continuity and asset guard
        run: |
          node - <<'NODE'
          const fs=require('fs');
          const s=require('./v50-friends-shots/src/script-data.json');
          const d=require('./v50-friends-shots/src/scene-data.json');
          const n=s.beats.length;
          if(!s.beats[0].narration.startsWith('三十五歳になって、友達が減った。')) throw new Error('Wrong canonical script loaded');
          if(n<60||n>135) throw new Error(`Unexpected fine-shot count ${n}`);
          if(d.length!==n) throw new Error(`Scene data mismatch ${d.length}/${n}`);
          if(new Set(s.beats.map(b=>b.visual)).size!==n) throw new Error('Every shot must have a distinct visual key');
          const repeated=[];
          for(let i=1;i<d.length;i++) if(d[i].phase===d[i-1].phase&&d[i].variant===d[i-1].variant) repeated.push(i);
          if(repeated.length) throw new Error(`Adjacent same-phase shots repeat foreground variant: ${repeated.join(',')}`);
          const phases=new Set(d.map(x=>x.phase));
          const required=['present_lonely','busy_excuse','baba_youth','career_start','mori_transfer','shirokane_defense','wedding_measure','own_transfer','kobayashi_house','different_game','avoidance','side_light','juniors_safe','meguro_buy','yurakucho_talk','screentime_alibi','spring_invite','no_grading','showwindow','backroom','return_baba','stairs_down','reunion','acceptance','walk_home','rotary_goodbye','revision_history','final_archive'];
          for(const p of required) if(!phases.has(p)) throw new Error(`Missing phase ${p}`);
          const assets=['BG_meguro_1ldk_night.svg','BG_takadanobaba_basement_izakaya.svg','BG_takadanobaba_izakaya_stairs.svg','BG_takadanobaba_rotary_night.svg','BG_sangenjaya_street_night.svg','BG_yokohama_hotel_wedding_reception.svg','BG_shizuoka_suburban_house_yard_day.svg','BG_yurakucho_yakitori_night.svg','BG_yurakucho_station_platform_night.svg','BG_storefront_showwindow_night.svg','PROP_taxi_black_split.svg','PROP_izakaya_beer_edamame_table.svg','PROP_minivan_white_angled.svg','PROP_small_dog.svg','PROP_small_vinyl_pool.svg'];
          for(const a of assets){const found=fs.existsSync(`./v50-friends-shots/public/assets/backgrounds/${a}`)||fs.existsSync(`./v50-friends-shots/public/assets/props/${a}`);if(!found)throw new Error(`Missing template asset ${a}`)}
          console.log(`V50 continuity architecture: ${n} short shots / ${phases.size} phases / no adjacent variant repeats`);
          NODE
      - name: Production QA pre
        run: node shared/qa/validate-video.mjs v50-friends-shots pre
      - name: Pronunciation regression tests
        run: node shared/voice/test-pronunciation.mjs
      - name: TypeScript check
        working-directory: v50-friends-shots
        run: npm run check
      - name: Start VOICEVOX
        run: |
          docker pull voicevox/voicevox_engine:cpu-latest
          docker run -d --rm --name voicevox -p 127.0.0.1:50021:50021 voicevox/voicevox_engine:cpu-latest
          for i in {1..90}; do
            if curl -fsS http://127.0.0.1:50021/version >/dev/null; then exit 0; fi
            sleep 2
          done
          docker logs voicevox || true
          exit 1
      - name: Generate measured narration
        working-directory: v50-friends-shots
        run: node scripts/generate-voicevox.mjs
      - name: Duration guard
        run: |
          node - <<'NODE'
          const s=require('./v50-friends-shots/src/sync-timing.json');
          console.log(`Measured duration: ${s.durationSeconds}s / ${(s.beats||[]).length} shots`);
          if(s.durationSeconds<480) throw new Error(`V50 is unexpectedly short: ${s.durationSeconds}s`);
          if(s.durationSeconds>1500) throw new Error(`V50 is unexpectedly long: ${s.durationSeconds}s`);
          NODE
      - name: Production QA post
        run: node shared/qa/validate-video.mjs v50-friends-shots post
      - id: plan
        working-directory: v50-friends-shots
        run: |
          MATRIX="$(node scripts/plan-segments.mjs)"
          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"
      - name: Pack prepared V50
        run: tar --exclude='v50-friends-shots/node_modules' -czf v50-prepared.tar.gz v50-friends-shots
      - uses: actions/upload-artifact@v4
        with:
          name: v50-prepared
          retention-days: 1
          if-no-files-found: error
          path: v50-prepared.tar.gz

  render:
    needs: prepare
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4
      - name: Install Japanese font
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk
          fc-cache -f
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: actions/download-artifact@v4
        with:
          name: v50-prepared
          path: .
      - run: tar -xzf v50-prepared.tar.gz
      - name: Install V50 dependencies
        working-directory: v50-friends-shots
        run: npm install
      - name: Render segment
        working-directory: v50-friends-shots
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: |
          mkdir -p out
          npx remotion render src/index.tsx V50FriendsShots "out/segment-${{ matrix.id }}.mp4" \
            --frames=${{ matrix.start }}-${{ matrix.end }} \
            --codec=h264 --crf=18 --concurrency=2 --log=warn
      - uses: actions/upload-artifact@v4
        with:
          name: v50-segment-${{ matrix.id }}
          retention-days: 1
          if-no-files-found: error
          path: v50-friends-shots/out/segment-${{ matrix.id }}.mp4

  finalize:
    needs: [prepare, render]
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Install ffmpeg and Japanese font
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-noto-cjk
          fc-cache -f
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: actions/download-artifact@v4
        with:
          name: v50-prepared
          path: .
      - run: tar -xzf v50-prepared.tar.gz
      - uses: actions/download-artifact@v4
        with:
          pattern: v50-segment-*
          path: v50-friends-shots/parts
          merge-multiple: true
      - name: Generate BGM
        working-directory: v50-friends-shots
        run: node scripts/generate-bgm.mjs
      - name: Concatenate segments
        working-directory: v50-friends-shots
        run: |
          mkdir -p out
          find parts -maxdepth 1 -name 'segment-*.mp4' -print | sort -V | while read -r f; do printf "file '%s'\n" "$(realpath "$f")"; done > out/concat.txt
          ffmpeg -y -loglevel error -f concat -safe 0 -i out/concat.txt -c copy out/video-only.mp4
      - name: Mix narration and BGM
        working-directory: v50-friends-shots
        run: |
          ffmpeg -y -loglevel error -i out/video-only.mp4 -i public/audio/narration.m4a -i public/audio/bgm.m4a \
            -filter_complex "[1:a]volume=1.0[vo];[2:a]volume=0.045[bg];[vo][bg]amix=inputs=2:duration=longest:dropout_transition=2[a]" \
            -map 0:v:0 -map "[a]" -c:v copy -c:a aac -b:a 192k -movflags +faststart -shortest out/v50-friends-shots-final.mp4
      - name: Create scene-complete QA contact sheet
        run: node shared/qa/generate-scene-contact-sheet.mjs v50-friends-shots v50-friends-shots/out/v50-friends-shots-final.mp4 v50-friends-shots/out/v50-friends-shots-contact-sheet.jpg
      - name: Create shot table
        working-directory: v50-friends-shots
        run: |
          node - <<'NODE'
          const fs=require('fs'); const s=require('./src/script-data.json'); const d=require('./src/scene-data.json'); const t=require('./src/sync-timing.json');
          let out='# V50 Fine Shot Table\n\n| Shot | Time | Phase | Shot type | Narration |\n|---|---:|---|---|---|\n';
          for(let i=0;i<s.beats.length;i++){const b=s.beats[i],m=d[i],z=(t.beats||[])[i]||{};const st=Number(z.start||0).toFixed(1),en=Number(z.end||0).toFixed(1);out+=`| ${b.id} | ${st}-${en}s | ${m.phase} | ${m.shotKind||''} | ${b.narration.replace(/\|/g,'／').replace(/\n/g,' ')} |\n`;}
          fs.writeFileSync('out/v50-friends-shots-table.md',out);
          NODE
      - name: Publish GitHub Release
        working-directory: v50-friends-shots
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VIDEO_NAME="knowledge-video-v50-friends-shots-${GITHUB_RUN_ID}.mp4"
          cp out/v50-friends-shots-final.mp4 "out/${VIDEO_NAME}"
          TAG="v50-friends-shots-${GITHUB_RUN_ID}"
          gh release create "$TAG" "out/${VIDEO_NAME}" out/v50-friends-shots-contact-sheet.jpg out/v50-friends-shots-table.md public/audio/pronunciation-report.json qa/preproduction-summary.json IMPLEMENTATION_PLAN.md src/script-data.json src/scene-data.json \
            --repo "$GITHUB_REPOSITORY" \
            --title "V50 Friends Fine-Shot Production ${GITHUB_RUN_ID}" \
            --notes "Fine-shot revision of 東京で友達が減ったのは、忙しくなったからじゃなかった。. Same-location shots now keep the background/camera clock continuous instead of restarting, while foreground objects/framing change per narration beat. Full-screen fades occur only on real phase/location changes. Canonical script begins with 三十五歳になって、友達が減った。."
